(function (){
    
    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        var NaijaState = String(element)
        var tmp = NaijaState.length
        for (let w = index; w > 0 && tmp < ; w++) {
            
            
        }
        
    }
})()